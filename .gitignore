# kdb+/q
*.log
expected/
actual/
*.qlog

# OS
.DS_Store

# local machine config (data paths etc.) - see .env for the expected keys
.env

# local Claude Code session permissions, not project config
.claude/settings.local.json

# local q/PeachQ binaries some contributors may keep alongside the repo
/q
/peachq/

# local qStudio jar kept alongside the repo to run it
/qstudio.jar

# build output from scripts/build.sh - regenerate on demand, don't commit it
/dist/

# run artifacts from scripts/timer_replay_example.q - regenerate on demand,
# don't commit it
/scripts/output/

# bytecode cache from running scripts/torq_demo.py directly
__pycache__/
*.pyc
