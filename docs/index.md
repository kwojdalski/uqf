---
title: "uqf documentation"
---

<!-- The site's home page. It holds no content of its own: the documentation
     map is README.md, which is what a reader browsing the repository on
     GitHub lands on, and duplicating it here would be two copies of the same
     taxonomy to keep in step.

     Without this file Quarto has no index for the project and redirects "/"
     to the first document it rendered - alphabetically, that was
     architecture/cryptorust-discovery.html, a single-decision note as the
     front door. Sidebar order does not change that; only an index does. -->

{{< include README.md >}}
