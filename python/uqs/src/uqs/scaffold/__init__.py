"""`uqs new-job`: writing a new pipeline's files for you.

`jobs.py` builds a plan, `plan.py` is the plan type and how it is applied,
`templates.py` holds the file bodies, and `scaffold/wizard.py` is the interactive
front end that collects the answers `jobs.py` needs.
"""
