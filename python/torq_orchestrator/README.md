# torq-orchestrator

CLI (`torq_demo.py`) and MCP server (`torq_demo_mcp.py`) for the vendored
TorQ Finance Starter Pack demo (see the repo root's `docs/torq-demo.md`).
Shared bootstrapping/config logic lives in `src/torq_orchestrator/core.py`.
