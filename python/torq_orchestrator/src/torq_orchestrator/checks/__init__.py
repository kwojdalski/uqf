"""Read-only inspections that answer "is the stack healthy?".

The smoke test over a running fleet, the HDB's on-disk shape, and the plant's
schema as the live processes actually report it. These are diagnostics: they
query and compare, and never change what they are looking at.
"""
